var br = "\n";
var v = prompt("変換したいメガバイトを入力してください（数値のみ）");

alert(
  v + "MBを変換" + br +
  "バイト（8ビット）:" + ( v * 1024 * 1024 ) + "B" + br +
  "キロバイト:" + ( v * 1024 ) + "KB" + br +
  "ギガバイト:" + ( v / 1024 ) + "GB" + br +
  "テラバイト:" + ( v / 1024 / 1024 ) + "TB"
);